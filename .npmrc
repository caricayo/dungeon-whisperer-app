# Ensure optional dependencies are installed
# Workaround for npm bug with rollup platform-specific binaries
# https://github.com/npm/cli/issues/4828
include=optional
audit=false
fund=false
# Force installation of optional dependencies
install-strategy=hoisted
@rollup:registry=https://registry.npmjs.org/